/**
 * Implement the Tree data structure for storing select options.
 *
 * Provide a function to convert the tree into a plain object (dictionary)
 * for easy serialization.
 *
 * Provide a function to restore a tree with its nodes for easy
 * deserialization.
 */
class MyTree {
  constructor (root) {
    this.root = root;
  }
}

module.exports = MyTree;
